{% extends 'base.html.twig' %}

{% block title %}{{ 'productpage.header'|trans }}{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="my-4 text-center">{{ 'productpage.title'|trans }}</h1>
        {{ component('ProductSearch') }}
    </div>
{% endblock %}