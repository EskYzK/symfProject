{% extends 'base.html.twig' %}

{% block title %}{{ 'orderindex.header' |trans }}{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>🛍️ {{ 'orderindex.header' |trans }}</h1>

    <table class="table table-striped table-hover mt-4">
        <thead>
            <tr>
                <th>{{ 'orderindex.reference' |trans }}</th>
                <th>{{ 'orderindex.date' |trans }}</th>
                <th>{{ 'orderindex.client' |trans }}</th>
                <th>{{ 'orderindex.price' |trans }}</th>
                <th>{{ 'orderindex.status' |trans }}</th>
                <th>{{ 'orderindex.actions' |trans }}</th>
            </tr>
        </thead>
        <tbody>
        {% for order in pagination %}
            <tr>
                <td>{{ order.reference }}</td>
                <td>{{ order.createdAt|date('d/m/Y H:i') }}</td>
                <td>{{ order.user.email }}</td>
                <td class="fw-bold">{{ order.total }} €</td>
                <td>
                    {% if order.status == 'payee' %}
                        <span class="badge bg-success">Payée</span>
                    {% else %}
                        <span class="badge bg-secondary">{{ order.status }}</span>
                    {% endif %}
                </td>
                <td>
                    <a href="{{ path('admin_order_show', {'id': order.id}) }}" class="btn btn-sm btn-info text-white">
                        <i class="fas fa-eye"></i> {{ 'orderindex.detail' |trans }}
                    </a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6" class="text-center">{{ 'orderindex.noorder' |trans }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="d-flex justify-content-center">
        {{ knp_pagination_render(pagination) }}
    </div>
</div>
{% endblock %}